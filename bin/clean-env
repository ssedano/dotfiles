#!/bin/zsh
source ~/.aliases
source ~/.functions
TOMCAT_PROCESS_ID=`sudo jps | grep Bootstrap | cut -d ' ' -f 1`


([ -n "$TOMCAT_PROCESS_ID" ] && sudo kill -9 $TOMCAT_PROCESS_ID

rabbitmq-reset
redis-cli flushall
database
cd /opt/tomcat && sudo ./bin/catalina.sh start)
